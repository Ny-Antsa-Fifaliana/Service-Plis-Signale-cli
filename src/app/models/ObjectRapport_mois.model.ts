export class ObjectRapport_mois{
    mois: string;
    livree: number;
    retour: number;

    constructor(mois:string, livree: number, retour: number){
        this.mois=mois;
        this.livree=livree;
        this.retour=retour;
    }
}